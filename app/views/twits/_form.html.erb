<%= form_with(model: twit) do |form| %>
  <% if twit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(twit.errors.count, "error") %> prohibited this twit from being saved:</h2>

      <ul>
        <% twit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :twit, style: "display: block" %>
    <%= form.text_area :twit %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
